apiVersion: v1
kind: Service
metadata:
  name: {{ include "alertmanager-uvdesk-agent.fullname" . }}
  labels:
    {{- include "alertmanager-uvdesk-agent.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "alertmanager-uvdesk-agent.selectorLabels" . | nindent 4 }}
