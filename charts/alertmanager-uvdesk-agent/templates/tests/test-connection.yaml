apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "alertmanager-uvdesk-agent.fullname" . }}-test-connection"
  labels:
    {{- include "alertmanager-uvdesk-agent.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "alertmanager-uvdesk-agent.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
